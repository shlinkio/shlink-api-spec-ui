import React, { FunctionComponent } from 'react';
import './LoadingSpec.css';

const LoadingSpec: FunctionComponent = () => (
  <div className="loading-spec">
    <h2>Loading {'Shlink\'s'} Open API spec...</h2>
  </div>
);

export default LoadingSpec;
